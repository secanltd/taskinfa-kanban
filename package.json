{
  "name": "taskinfa-kanban",
  "version": "2.0.1",
  "private": true,
  "description": "Autonomous task automation system — kanban dashboard + Telegram bot + Claude Code orchestration",
  "author": "SECAN",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "dashboard:dev": "npm run dev --workspace=packages/dashboard",
    "telegram:dev": "npm run dev --workspace=packages/telegram",
    "orchestrator": "npx tsx --env-file=.env scripts/orchestrator.ts",
    "build:orchestrator": "node scripts/build-orchestrator.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "tsx": "^4.7.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
